let numero = +prompt(`Ingresa un numero`);

let mensaje;
if (numero > 0) {
  mensaje = "1";
} else if (numero == 0) {
  mensaje = "0";
} else {
  mensaje = "-1";
}
alert(mensaje);

let resultado;
(a + b > 4) ? "Debajo" : "Encima";
console.log(resultado);


let texto = (login == 'Empleado') ? 'Hola' :
(login == 'Director') ? 'Felicidades' :
(login == '') ? 'Sin sesion' :
'';

let age
// if (!(age >= 14 && age <=90))

// if (age < 14 || age > 90)